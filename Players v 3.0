import java.io.IOException;

public class Player {

	public String player() throws IOException {
		String player = ConsoleUI.promptForInput("Assign name for player", false);

		return player;
	}

	public void pieceSelection() throws IOException {
		PlayerPieces piecesSelect = new PlayerPieces();
		System.out.println("Players will now select their pieces");

		piecesSelect.playerPieceSelect();

		piecesSelect.namesPieces();
	}

}
